#include "main.h"
/**
* print_last_digit - prints the last digit of a number
* @r: takes as parameter
* Return: Always (0)
*/

int print_last_digit(int r)
{
	int result;

	result = r % 10;
	return (result);
}
