/**
* reverse_array- reverses a string
* @a: first string
* @n: size of string
* Return: A pointer to an integer
*/
void reverse_array(int *a, int n)
{
	int i;
	int rev[n];

	for (i = n; i > 0; i++)
	{
		rev[i] = a[i];
	}
}
